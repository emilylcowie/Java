package Beginner.Operators;

public class TernaryOperator {

    public static void main(String[] args) {
        int income = 120_000;
        String className = income > 100_000 ? "First" : "Economy"; // the ? is the ternary operator
        // puts a whole if statement into a single line.
    }
}
